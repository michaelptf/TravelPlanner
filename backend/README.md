# TravelPlanner Backend

Quickstart:

1. Copy `.env.example` to `.env` and fill values.
2. Install deps: `npm install`.
3. Run in dev: `npm run dev`.

This backend is a minimal Express skeleton. It will validate JWTs from Supabase and expose API routes for trips, schedules, expenses, and AI jobs.